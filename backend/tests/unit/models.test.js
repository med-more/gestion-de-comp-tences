const Competence = require('../../models/Competence');

describe('Modèle Compétence', () => {
  it('devrait être une fonction (constructeur)', () => {
    expect(typeof Competence).toBe('function');
  });
  // Ajoute ici d'autres tests unitaires selon les méthodes du modèle
});
