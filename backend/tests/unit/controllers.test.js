const competenceController = require('../../controllers/competenceController');

describe('Contrôleur Compétence', () => {
  it('devrait exister', () => {
    expect(competenceController).toBeDefined();
  });
  // Ajoute ici d'autres tests unitaires selon les fonctions exportées
});
